using UnityEngine;
using System.Collections;

public class SaveReset : MonoBehaviour
{
	public void OnClick()
	{
		/*
		var dataEvent = new SkillEvent();
		dataEvent.AddString("EventType", "ResetLevel");
		dataEvent.Save();
		*/

	}
}

